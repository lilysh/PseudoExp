clear();

%=== TO DO ===

%create a csv or txt file with block sequences
%fopen the legend file
%fgetl to get the line
%split the line by commas into a vector
%call pseudoExp with list of params

%web app generates param files

pseudoExp(0, 'S01', 1, 'practice', 'word', 'one', 0);